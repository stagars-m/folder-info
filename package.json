{
  "name": "folder-info",
  "version": "1.0.1",
  "description": "Obsidian plugin that shows separate file and folder counts in the native File Explorer.",
  "main": "main.js",
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "check": "npm run test && npm run build && node --check main.js",
    "verify-release": "node scripts/verify-version.mjs"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "file-explorer",
    "folders",
    "counts"
  ],
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=22"
  }
}
